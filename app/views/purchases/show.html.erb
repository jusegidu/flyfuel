<h1 class="text-primary">Detalles de tu compra</h1>
<div class="container ">
  <div class="text-center">
    <p class="h5">Estación de servicio: <%= @purchase.gas_station_product.gas_station.name %></p>
    <p class="h5">Dirección: <%= @purchase.gas_station_product.gas_station.address %></p>
    <p class="h5">Combustible: <%= @purchase.gas_station_product.name %></p>
    <p class="h5"> Cantidad: <%= @purchase.quantity %> gal</p>
    <p class="h5"> Total: $ <%= @purchase.amount %></p>


    <li>
          <a href="#" target="_blank">
            <%= link_to "Cancelar",
                  purchase_path(@purchase),
                  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
          </a>
        </li>


    <%= link_to "Editar compra", edit_purchase_path(@purchase)%>
  </div>

  <div class="container">
    <% if user_signed_in? == false %>
    <%= link_to "Login", new_user_session_path, class: "nav-link" %>
    <% end %>
  </div>

  <div class="rounded-5 card m-5 p-5  border-primary ">
    <%= render "payments/form" %>
  </div>
</div>
